import { EmailVerificationPending } from "@/components/auth/EmailVerificationPending";

/**
 * Email verification pending page route component
 * Shows after successful registration, prompting user to check email
 */
export default function EmailVerificationPendingPage() {
  return <EmailVerificationPending />;
}
