import { RegistrationPage as RegistrationComponent } from "@/components/auth/RegistrationPage";

/**
 * Registration page route component
 * Wraps the RegistrationPage component for routing
 */
export default function RegistrationPage() {
  return <RegistrationComponent />;
}
