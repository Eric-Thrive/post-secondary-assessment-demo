
}.end();clientt aially {
  awage);
} fin error.messError:',.error('❌ nsole co {
 error)atch ( c  
}}
    });
  Never'}`);
_login || '.lastgin: ${userst Lo.log(`   La    console);
  t}`d_aatere: ${user.ceated`   Crle.log(conso    );
  password}`s_${user.haassword: Has Pole.log(`   ons c   }`);
  is_activeser.e: ${uActivog(`   sole.l    cond}`);
  il_verifieer.emaified: ${usail Ver   Emnsole.log(`   co  le}`);
 user.ro Role: ${  g(`ole.lo    cons}`);
  user.email${il: `   Emansole.log(co;
      ername}`).ususername: ${  Userog(` console.l  s:`);
    il}. User Deta{index + 1g(`\n$le.lo   conso   dex) => {
r, inorEach((userows.fsult.:`);
    reh} user(s).rows.lengtsult ${re👤 Foundle.log(`\n  conso{
  
  } else });
    
    tive}`);s_acr.ie: ${use}) - Activ{user.email($sername} er.u. ${us 1}`${index +og(.lonsole  c {
    index) =>Each((user, ws.forResult.ro   adminsers:`);
 admin u system_ws.length}.rominResult ${adundlog(`\n📊 Foconsole. 
          `);
 SC
 DE created_atRDER BY     O'
 dminem_asyste = 'E rol WHER
     OM users    FRctive 
   , role, is_aname, emailCT userLE`
      SEy(t.querclien= await Result in   const admrs
 admin use system_ Check for    //
    
pa"');pipg "il containinname or emad with usero user foun❌ Nog('e.l    consol= 0) {
ngth ==rows.leif (result.  
  
);pa%'
  `pip'%il ILIKE pa' OR ema'pipe ILIKE HERE usernam
    Wsers 
    FROM ulogind_at, last_    create
  sword,as has_pasLL d IS NOT NU    passwor
  s_active, , i, rolel_verifiedmail, emairname, ese   id, u 
     SELECT  
ry(`ient.que= await clsult onst reuser
  cippa for P  // Check se');
  
 databad toctene'✅ Con.log(  consoleonnect();
t.cit clienwatry {
  aRL 
});

E_Uenv.DATABAScess.nString: pronnectiont({ 
  co= new Clie client 
constom 'pg';
nt } frort { Clieimp