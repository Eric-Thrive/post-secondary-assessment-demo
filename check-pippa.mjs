}();
ient.end await cl{
 } finally ;
e)rror.messag❌ Error:', error('console.eor) {
  (err catch  
} }
 );
    });
 | 'Never'}`t_login |ser.lasLogin: ${u   Last g(`.lolesoon     c}`);
 _atedr.creatd: ${use`   Createonsole.log( cd}`);
     _passwor${user.hasord: sswg(`   Has Pale.lo conso  e}`);
   iver.is_act${us Active: e.log(`   consol   ed}`);
  email_verifiied: ${user. Verifil Ema  log(`sole.  con);
    {user.role}``   Role: $log(console.}`);
      ailser.em  Email: ${u.log(`    console);
   rname}`: ${user.use   Usernamensole.log(`   co
   :`);etailsser D. U${index + 1}\n.log(`    consoleex) => {
  (user, ind.forEach(t.rowsules`);
    r(s):serngth} ult.rows.leund ${resu\n👤 Foe.log(`  consol{
  lse   } e});
    

    `);ve}is_actiive: ${user.}) - Actailuser.em} (${ername${user.usx + 1}. de(`${ine.log    consol> {
  ) =r, indexh((usews.forEaclt.roesu  adminRrs:`);
  m_admin usesysteows.length} Result.r${admin📊 Found log(`\n    console. `);
    
   SC
ated_at DERDER BY cre
      Om_admin'= 'systeERE role    WHs 
      FROM usertive 
   , is_acleroil, ma username, e     SELECTuery(`
 ent.qwait cliesult = at adminRnsrs
    codmin usestem_aeck for sy Ch //   
    
); "pippa"'ntainingemail cosername or h ur found witseog('❌ No unsole.l   co 0) {
 length ===ows.(result.r if 
 );
  a%'
  `LIKE '%pipp' OR email IppapiE ' ILIKameHERE usernrs 
    W   FROM usegin
 _at, last_lo  createdsword,
    as has_pasULL rd IS NOT Npasswo      
ve, ole, is_actirified, r, email_veame, email usern    id,SELECT 
  `
    t.query(lient = await cresulconst a user
  ck for Pipphe// C
  se');
  ed to databag('✅ Connectole.lons
  cot();nt.connec await clie
 

try {L 
});E_URBASDATA.env.: processnStringctio({ 
  conneient new Clnst client =
co';
 from 'pgient } Climport {