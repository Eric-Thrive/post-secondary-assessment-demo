;
em()yst
testEmailS  }
}
t(1);
xis.e
    proces);sage error.mesrror:',r('❌ Eerronsole.   co {
 (error: any)  } catch (0);
xits.e   proces 
     }
   ;
ole}`)ole: ${mv.r`  Rog(sole.l
      con); No'}`✅ Yes' : '❌hasToken ? ' ${mv.ken:ion Toicats Verifog(`  Ha  console.lo'}`);
    s' : '❌ Nied ? '✅ YeilVerif ${mv.emal Verified:ai.log(`  Em  console;
    il}`)${mv.emag(`  Email: ole.lo  cons    rname}`);
 ${mv.useme:  Userna(`ole.log
      consers[0];= mvUsonst mv {
      c
    } else  found');notMV user   ❌ .log('onsole   c== 0) {
   ength =.lers(mvUsf     i    

'));ername, 'MVs.us(eq(userers).where.from(us })
   sers.role role: u    onToken,
 ficatiilVeriusers.emaToken:   hased,
    Verifirs.email useailVerified:   em,
   .emailrsuse     email: 
 name,usere: users.sernam    ut({
  it db.selec= awamvUsers    const  user:');
 MVChecking '🔍 onsole.log(user
    c MV heck  
    // C
  e.log('');   consol
    }
  });;
     email})`)} (${admin.min.username- ${ad.log(`     ole   cons     min => {
ach(adadmins.forE      s):`);
stem admin(s.length} sydminFound ${a✅ `  log(e.onsol{
      c  } else .');
  entnot be sions cannotificatmin ans ad This meg('  console.lo;
     d!')ns founm admi ❌ No systensole.log('     co
  h === 0) {ns.lengtif (admi  
    min'));
  em_adsystrole, 'rs.se(u(eqheres).wm(user.fro   })ole
 sers.r   role: uemail,
   ers.il: us
      emaame,.usernuserssername: 
      u: users.id,     id
 select({b.= await dadmins nst 
    cors:');ratotem Administ👤 Sysog('le.l consodmins
   ystem aheck for s // C;
    
   g('') console.lo   t set');
'❌ No_EMAIL || DGRID_FROMenv.SENrocess. Email:', plog('  From   console.set');
 t' : '❌ Not Y ? '✅ SeRID_API_KEnv.SENDGss.e', procePI Key:e.log('  A    consol);
:'ionConfigurat📧 SendGrid og('   console.luration
 onfigid cGr Send // Check;
    
   on...\n')urationfigg system c('🔍 Checkinsole.logy {
    con
  trSystem() {ailction testEm
async fun();
.config;

dotenvotenv'env from 'drt * as dotorm';
impozzle-ri 'deq } from
import { d/schema';om '@share fr { users }importb';
r/dserve/apps/'.b } from port { dim